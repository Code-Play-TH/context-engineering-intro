# Lightweight requirements for KOL Management System
# Core dependencies only, without heavy ML/AI libraries

# FastAPI and web framework
fastapi[all]==0.104.1
uvicorn[standard]==0.24.0

# Database and ORM
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Pydantic V2
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# Redis and caching (compatible with Celery 5.3.4)
redis==4.6.0

# Celery for background tasks (optional for initial testing)
celery[redis]==5.3.4

# HTTP client
httpx==0.25.2
# Fixed aiohttp version for compatibility
aiohttp==3.8.5

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Utilities
python-dotenv==1.0.0
pytz==2023.3
jinja2==3.1.2
email-validator==2.1.0

# Monitoring and logging
structlog==23.2.0

# Development tools
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0

# Note: Removed heavy dependencies:
# - torch, transformers, scikit-learn (AI/ML libraries)
# - discord.py, line-bot-sdk (can be added later if needed)
# - Social media APIs (can be added later)
# - Advanced monitoring tools